import React from 'react';
import styles from './index.module.css';

export const Footer = () => {
  return (
    <div className={`${styles.footer_container}`}>
      <h5 className={`${styles.footer_label}`}>2022-parent©️futureitsuki</h5>
    </div>
  )
}